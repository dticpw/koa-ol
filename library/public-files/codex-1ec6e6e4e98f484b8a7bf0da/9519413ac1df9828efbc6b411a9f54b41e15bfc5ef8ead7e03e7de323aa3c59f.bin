# MA3 v0.3 · 本机 Agent 工作台

入口：**http://127.0.0.1:8763**。后台已作为 Linux 用户服务运行，并设置为登录图形桌面后自动启动。关闭网页或中控聊天不会停止 tmux 中的执行器。

这是中控在场的工作台：中控聊天负责理解需求与判断，后台负责任务记录、回执、通知和控制接口，tmux 承载真实执行 Agent。网页已经连接实际任务，尚未内置可直接对话的中控 Agent。

## 日常使用

1. 在中控聊天中描述任务，由中控准备带交付要求和独立验收命令的任务 JSON。
2. 网页点击「新任务」，选择定义与明确模型。勾选「仅准备任务」不会调用模型，之后可在工作动态中启动。
3. 执行时可看到真实 GNOME Terminal / tmux；网页显示任务动态与消息回执。「终端记录」在当前标签激活时定期更新，「打开终端」用于直接交互。
4. 首次新目录的信任提示仍需中控或用户核对并确认。网页不会自动识别所有 CLI 弹窗。
5. Agent 提交后，在「交付产物」运行独立验收，再「确认通过」。操作完成与任务验收通过是不同状态。
6. 后续可新建实例，或先结束已完成实例的旧终端，再在「新任务」中选择继续其 Codex 会话。旧任务和产物在实例历史中保留。

消息分为保存、投递、确认回执三步。只有确实查看实时终端、确认输入框空闲后才投递；投递不等于接收。工作台不会自动重发、猜测权限提示或换模型。

## 项目、实例、任务

| 对象 | 本版含义 |
|---|---|
| 项目 | 登记已有目录与名称，用于分组；不会移动文件，也不会自动进入该项目仓库开展工作 |
| 实例 | 一个执行 Agent 的工作历史，可以关联多项任务和同一段 Codex 对话 |
| 任务 | 独立的 run_id、attempt、工作目录、任务快照、消息与验收证据 |

所有任务当前在 `runs/<run_id>/work` 中运行。直接修改已有仓库、Git worktree 隔离、任务依赖图和无人值守调度尚未实现。Claude 启动适配保留，但未做真实模型验收；续接当前仅支持 Codex。

## 服务控制

下面查询后台状态，不修改任务：

```bash
systemctl --user status ma3-workbench.service
```

下面重启工作台接口；tmux 执行器保留，网页会自动重连：

```bash
systemctl --user restart ma3-workbench.service
```

下面关闭工作台并取消自动启动；**不会结束已有 tmux 任务**，结束任务请用网页「结束会话」或原有 `ma3.py stop`：

```bash
systemctl --user disable --now ma3-workbench.service
```

重新启用并立即启动：

```bash
systemctl --user enable --now ma3-workbench.service
```

自动启动依赖本机 GNOME 图形会话，未设置开机无登录常驻，也未做本轮真实重启验证。操作系统重启后 CLI 进程消失，但任务记录和 Codex 保存的会话仍可查看；后台不会自动重放未完成操作。

服务单元源码位于 `deploy/ma3-workbench.service`，已安装到 `~/.config/systemd/user/`。`KillMode=process` 是有意配置：停止后台不连带杀死 tmux 后代。后台的任务操作是串行执行，验证命令也会进入操作队列；观察与网页读取独立进行。

## MCP 与 Skill

Linux Codex 已注册 `ma3` stdio MCP：`ma3_overview`、`ma3_task`、`ma3_terminal`、`ma3_job`、`ma3_action`。它调用独立后台，MCP 客户端断开不停止服务。

共享 Skill 源码：`/mnt/c/Users/DTICPW/agent-workflows/skills/ma3-control/SKILL.md`。它教中控如何使用这组工具，不承载后台状态。虽然源文件两系统可见，运行时明确仅支持 Linux。

新 MCP 与 Skill 需要**新开 Codex 会话**发现；当前会话可以先用本地客户端：

```bash
/home/dticpw/conda/envs/py310/bin/python /home/dticpw/PG_linux/MA3/workbench_client.py snapshot
```

这是只读概览；`detail <run_id>`、`terminal <run_id>`、`job <job_id>` 分别查询任务、终端和异步操作。`action <payload.json>` 会执行文件中的操作；接口详情见 [接口说明](V0.3接口与运行.md)。

只注册了 Linux Codex；Claude Code 与 Windows MCP 配置未改。MCP 原配置备份在 `~/.codex/config.toml.before-ma3-20260910`，移除本次接口用 `codex mcp remove ma3`，无需恢复整个旧配置。

## 存储与边界

- `runs/` 是任务与验收证据；`.ma3/workspace.json` 是项目、实例映射、收件箱和操作记录。备份时两者一起保留。
- 只监听 `127.0.0.1:8763`，校验 Host、Origin 与写请求头；无公网、手机中继、VPS 或云端同步。
- 产物预览只返回原文，Agent 生成的 HTML 不会在工作台控制接口的域内运行。「打开」使用本机系统程序。
- 本机同一用户下的程序可以调用控制接口。CLI 沿用现有本机权限；工作目录隔离不是操作系统安全沙箱。
- 重启时未完成的后台操作标记 interrupted，不自动重试；请检查实际任务后决定下一步。

## 验证与当前现场

[验收报告](V0.3验收报告.md)记录 44 项自动化测试、五种宽度的浏览器检查、真实模型任务、MCP 与后台重启实测。

首次网页派工已通过独立验收。续接任务已恢复同一 Codex 对话、生成正确记忆标记，但随后遇到 **429 限流**，尚无 submit 回执；保留为执行中 / 等待输入，不能记为完整验收成功。

以下测试使用临时目录，不调用模型：

```bash
/home/dticpw/conda/envs/py310/bin/python -m unittest -q test_workbench test_orchestrator test_protocol
```

旧命令行流程见 [v0.2 使用说明](archive/README_v02.md)、[施工与协议](V0.2施工与协议.md)。之前的 [研究方案](index.html)、[v0.2 验收报告](V0.2验收报告.md)和冒泡排序产物全部保留。
