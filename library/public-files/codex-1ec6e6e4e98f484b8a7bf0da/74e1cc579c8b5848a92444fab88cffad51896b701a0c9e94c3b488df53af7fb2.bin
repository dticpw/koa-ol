<!doctype html><html lang="zh-CN"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>V0.3验收报告.md</title><style>/* muQ theme contract v1: semantic roles shared by catalogue, reader, reports. */
:root{--paper:#f5f1eb;--white:#fffdf9;--ink:#302936;--muted:#716772;--plum:#665173;--night:#292332;--gold:#dfcba2;--line:#dfd7dc;--muq-reading-width:78ch;--muq-radius:8px;--muq-space:24px;--muq-body-font:"Noto Sans CJK SC","Microsoft YaHei",system-ui,sans-serif;--muq-heading-font:"Noto Serif CJK SC","Songti SC",serif}
/* Namespaced report components, v1. Safe to embed in a standalone report. */
.muq-document{box-sizing:border-box;max-width:var(--muq-reading-width,78ch);margin:40px auto;padding:0 24px;color:var(--ink,#302936);font:16px/1.95 var(--muq-body-font,system-ui);overflow-wrap:anywhere}
.muq-document h1,.muq-document h2,.muq-document h3{font-family:var(--muq-heading-font,serif);line-height:1.5;margin:1.8em 0 .6em}.muq-document h1{font-size:2em}.muq-document h2{font-size:1.5em;border-bottom:1px solid var(--line);padding-bottom:.4em}.muq-document a{color:var(--plum);text-decoration:underline}.muq-document pre{padding:18px;background:var(--paper);overflow:auto;white-space:pre-wrap}.muq-document code{font-size:.9em}.muq-document table{display:block;overflow:auto;border-collapse:collapse;max-width:100%}.muq-document th,.muq-document td{border:1px solid var(--line);padding:10px 14px;text-align:left}.muq-document img{max-width:100%;height:auto}.muq-document blockquote,.muq-callout{margin:24px 0;padding:16px 22px;border-left:3px solid var(--plum);background:var(--paper)}.muq-lead{font-size:1.15em;color:var(--muted)}.muq-source{font-size:.85em;color:var(--muted)}.muq-document :focus-visible{outline:3px solid var(--plum);outline-offset:3px}@media(max-width:540px){.muq-document{margin:20px auto;padding:0 16px;font-size:15px}}@media print{.muq-document{max-width:none;margin:0}.muq-document a{color:inherit}}
</style><main class="muq-document"><h1>MA3 v0.3 验收报告</h1>
<p>日期：2026-09-10。工作目录：<code>/home/dticpw/PG_linux/MA3</code>。</p>
<p><strong>工作台、独立后台、MCP 和中控 Skill 已可用。</strong> 网页真实派工、独立验收、后台重启保留执行器均已通过。Codex 会话续接已恢复原上下文并生成正确结果，但随后被 429 限流阻断，尚未提交，不能把续接整项任务写成完整通过。</p>
<p>入口：<strong>http://127.0.0.1:8763</strong>。</p>
<h2>本轮交付</h2>
<table>
<thead>
<tr>
<th>部分</th>
<th>已实现</th>
</tr>
</thead>
<tbody>
<tr>
<td>本机后台</td>
<td>用户级 systemd 服务、单实例锁、持久项目/实例/操作注册表、约 2 秒观察、SSE、错误与通知</td>
</tr>
<tr>
<td>网页工作台</td>
<td>三列布局；项目与实例、任务动态、消息和回执、产物与独立验收、终端记录、收件箱、后台操作</td>
</tr>
<tr>
<td>实际控制</td>
<td>准备/启动任务、保存/投递消息、打开/结束指定终端、预览/打开产物、验收、登记项目、实例历史</td>
</tr>
<tr>
<td>对话续接</td>
<td>使用真实 Codex 会话 UUID，保留实例，建立新的任务目录和回执 attempt，避免同一实例双开</td>
</tr>
<tr>
<td>MCP</td>
<td>5 个工具，stdio ↔ 本机 HTTP；幂等操作 ID、异步查询、错误回传</td>
</tr>
<tr>
<td>中控 Skill</td>
<td><code>ma3-control</code>，存于共享 Skill 源码库，明确仅限 Linux 运行</td>
</tr>
</tbody>
</table>
<p>沿用 jar.png 的三列结构，采用深蓝顶栏、浅色面板和明确的状态颜色。所有计数、任务和事件均来自本机记录，不展示虚构的配额或在线 Agent。</p>
<h2>验收证据</h2>
<h3>自动化与浏览器</h3>
<ul>
<li><strong>44 项测试通过</strong>：原有 33 项协议测试，加 11 项工作台测试；覆盖操作幂等、冲突 ID、失败持久化、重启中断标记、项目/实例映射、续接实时检查、模型保留、产物越界/符号链接拒绝、HTTP Host/Origin/请求头、纯文本预览、SSE 和 MCP 错误语义。</li>
<li>使用已安装 Playwright 与系统 Chrome，无新增依赖或浏览器下载。</li>
<li>在 <strong>1440、1024、768、390、360 px</strong> 五种宽度检查，无页面横向溢出。</li>
<li>实测搜索、项目筛选、实例历史、仅准备任务、产物原文预览、终端在线/离线标记、键盘打开/关闭对话框、通知已读与刷新后保留。</li>
<li>最终浏览器检查：<strong>脚本错误 0、资源错误 0</strong>。首次检查发现 favicon 404，已修复并复查。</li>
<li>429 现场显示为等待输入；未提交任务的「确认通过」不可用。</li>
</ul>
<p>证据：<a href="preview/v03/final-browser-results.json">最终浏览器结果</a>、<a href="preview/v03/final-1440.png">桌面截图</a>、<a href="preview/v03/final-390.png">窄屏截图</a>。这不是远程手机入口；窄屏检查用于窗口自适应。</p>
<h3>真实派工、重启与独立验收</h3>
<p>首任务：<code>task-20260910-162054-58566e</code>。</p>
<p>通过网页先准备、再启动真实 <code>gpt-5.6-sol</code>。可见 tmux / Codex 出现新目录信任提示，中控核对目录后只确认一次，留有 note。Agent claim、编写 <code>words.py</code> 与 <code>result.json</code>、submit，随后网页运行独立检查，覆盖 10 组输入和固定 JSON 标记；accept 后状态为 <strong>verified</strong>。</p>
<p>执行期间重启 <code>ma3-workbench.service</code>，确认同一 CLI PID 仍存活、tmux 仍可进入，已打开网页自动重连并收到新后台启动时间。MCP 测试进程退出后，独立后台仍正常响应。</p>
<p>证据：<a href="runs/task-20260910-162054-58566e/acceptance.json">首任务验收</a>、<a href="preview/v03/restart-results.json">后台重启结果</a>、<a href="preview/v03/first-accepted.png">网页验收截图</a>、<a href="preview/v03/mcp-results.json">MCP 测试</a>。</p>
<h3>续接：已验证的部分与未完成的部分</h3>
<p>第二任务：<code>task-20260910-162411-972bcb</code>。</p>
<p>网页结束首任务的旧终端，再选择继续同一实例，启动新任务。实际命令包含 <code>codex resume 01a08a68-da5e-7671-ab20-bc096dc33f34</code>，使用新 work 目录和新 attempt。实例历史同时包含两项任务，没有误建第二个实例。</p>
<p>恢复后的 CLI 显示完整旧对话。第二任务没有提供上一任务的标记；Agent 从上下文正确生成 <code>resume-proof.json</code>，其标记与首任务一致。只读独立检查也确认了输出内容。</p>
<p>随后终端出现 <code>exceeded retry limit, last status: 429 Too Many Requests</code>。这里的重试是 CLI 内部行为；中控没有再次投递、没有换模型。<strong>第二任务当前仍为 claimed / waiting_input，没有 submit，也没有 accept。</strong> 只读检查结果单独存为预览证据，未冒充协议验收记录。</p>
<p>证据：<a href="preview/v03/resume-results.json">续接启动与状态</a>、<a href="preview/v03/resume-output-check.json">产物只读检查</a>、<a href="runs/task-20260910-162411-972bcb/terminal.txt">现场终端</a>。</p>
<p>后续在限流解除、明确决定继续后，由中控查看实时输入框，再保存并投递一次继续消息，等待 ack/submit，最后 validate/accept；不要直接把现在的任务改为已通过。</p>
<h2>部署状态</h2>
<ul>
<li>用户服务 <code>ma3-workbench.service</code> 为 <strong>active / enabled</strong>，登录 GNOME 图形桌面时启动；本轮未注销或重启机器，因此新登录自启仍待实际验证。</li>
<li>仅绑定 <code>127.0.0.1:8763</code>；没有公网、手机中继、VPS 配置。</li>
<li><code>KillMode=process</code> 有意保留 tmux 子进程。重启时 systemd 可能记录 left-over process 提示，这是该保留行为的结果；已实际验证执行器继续运行。</li>
<li>单实例锁已实测：启动另一份后台会明确拒绝，不改写原后台元数据。</li>
<li>Linux Codex 已注册 <code>ma3</code>，其他既有 MCP 条目和非项目全局设置核对未变。CLI 对新任务目录另行写入了信任记录。未修改 Windows 或 Claude Code MCP 配置。</li>
<li>Skill 格式校验通过；校验器缺少的 PyYAML 从本机已有系统库加载，未安装包。新 Skill/MCP 在新开 Codex 会话后发现；当前会话可用本地客户端。</li>
</ul>
<h2>本版边界</h2>
<p>网页尚未内置中控聊天，用户仍在当前聊天入口提需求。项目登记仅用于归属分组，任务仍使用 MA3 独立工作目录；还没有现有仓库编辑 / worktree / 合并流程。没有任务依赖图、资源调度、自动模型切换或无人值守重试。Claude 未做真实模型验收，也未实现续接。</p>
<p>下一轮适合先处理限流后的续接提交，再选一个真实项目接入仓库工作区与审阅流程。待这条路径稳定后，再考虑把中控对话嵌入工作台。</p>
<p>完整机器可读证据：<a href="v03-verification.json">v03-verification.json</a>。使用方法：<a href="README.md">README</a>；接口细节：<a href="V0.3接口与运行.md">接口与运行</a>。</p></main></html>