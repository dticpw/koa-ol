# MA3 第一版验收报告

日期：2026-09-09。运行：`bubble-20260909-080709-b1d4`。

**技术闭环通过。** 当前会话担任中控，在可见 GNOME Terminal 的 tmux 中启动真实 Codex 交互会话，显式选择 `gpt-5.6-sol medium`。子 Agent 完成任务领取、Python 算法、HTML 展示、浏览器打开和最终提交，中控独立验证后将状态标记为 verified。

## 查看结果

- [冒泡排序展示页](runs/bubble-20260909-080709-b1d4/work/index.html)
- [实际 Python 源码](runs/bubble-20260909-080709-b1d4/work/bubble_sort.py)
- [子 Agent 回执内容](runs/bubble-20260909-080709-b1d4/work/result.json)
- [状态与事件](runs/bubble-20260909-080709-b1d4/state.json)
- [中控验收数据](runs/bubble-20260909-080709-b1d4/acceptance.json)
- [终端执行记录](runs/bubble-20260909-080709-b1d4/terminal.txt)
- [桌面截图](runs/bubble-20260909-080709-b1d4/desktop.png) / [手机截图](runs/bubble-20260909-080709-b1d4/mobile.png)

## 实测结果

| 检查 | 结果 |
|---|---|
| 真实 CLI | Codex v0.153.4，gpt-5.6-sol medium，tmux 3.2a |
| 可见终端 | tmux list-clients 确认有终端客户端连接 |
| 回执 | 子 Agent 先 claim，后 submit；未用终端文案替代状态回执 |
| Python | 中控独立 1,393 组整数数组通过；不修改输入、返回新列表；稳定性检查通过 |
| 算法实现 | 人工检查为相邻比较交换，带提前退出，无内置排序替代 |
| 源码一致性 | 浏览器显示的完整源码与 Python 文件一致 |
| 浏览器 | Chrome 152.0.7977.82；1440/1024/768/390/360px 无页面横向溢出 |
| 交互 | 9 步排序演示、最终升序、重置、Enter/右箭头/R、移动端操作和减少动效均通过 |
| 资源 | 无控制台错误、无加载失败；自包含，无外部字体/脚本/图片 |
| 打开展示 | 子 Agent 实际调用 xdg-open，返回 0，输出 Opening in existing browser session；修正后再次打开 |
| 控制器 | 8 项协议测试通过，包括旧 attempt、重复回执、缺失文件、提交后篡改与验收证据绑定 |

验收数据绑定最终三个交付文件的 SHA256；算法、页面测试时的文件哈希与最终提交一致。源文件使用 LF。

## 本次中控介入

1. 核对刚创建的目录后，确认一次 Codex 信任提示；Codex 自身可能记录目录信任。
2. 独立浏览器测试发现 1024px 时页面宽度为 1195px，交回子 Agent 修正 Grid 固有最小宽度。
3. 子 Agent 的 Playwright MCP browser_tabs 长时间未返回，中控按一次 Esc 中断；确认空闲输入框后发送修正文件。codex queue 虽返回入队成功，但本次未证实自动消费，不能称为可靠送达。
4. 子 Agent 修正页面、澄清静态检查与浏览器实测的区别、重新打开页面并提交；中控复测通过。

具体记录见 [介入记录](runs/bubble-20260909-080709-b1d4/interventions.json) 与 [修正要求](runs/bubble-20260909-080709-b1d4/修正要求.md)。后续 start 的任务模板已加入不调用浏览器 MCP、由中控独立检查的约定。

## 结论边界

这是中控在场的单子 Agent 闭环，尚非无人值守系统。没有自动就绪识别、超时检测、自动恢复、多写者 worktree 或权限隔离。终端和浏览器已实际启动，但“用户亲眼看到并满意”的体验确认没有代为填写。

未安装依赖，未修改 MA2，未提交或推送 Git。终端会话保留，可继续查看和交互。

## 重新进入终端

下面这条命令连接仍运行的 tmux 会话，不创建新任务、不再次产生任务调用：

```bash
tmux -L ma3 attach -t bubble-20260909-080709-b1d4
```

按 Ctrl+B，松开后按 D 可退出查看并保留会话。第一版启动及维护方式见 [README](README.md)，实施约定见 [施工方案](施工方案.md)。
