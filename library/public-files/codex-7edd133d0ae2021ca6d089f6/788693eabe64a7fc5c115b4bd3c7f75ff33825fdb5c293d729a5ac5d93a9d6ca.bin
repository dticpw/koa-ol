# AI Chat 三模型接入 · 本地实现与上线清单

## 用户确定的范围

- 网页 /chat/：GPT（gpt-5.6-sol）、DeepSeek（请求 deepseek-chat）、Qwen（现有 qwen-plus）。
- 外部 /ai/v1/models 与 /ai/v1/responses：只提供 gpt-5.6-sol。
- 三个网页模型经网站后端调用，上游分别为现有 GPT 服务、DeepSeek 官方和阿里云官方。继续写入现有 chat_logs，Stats 无须数据库迁移。

## 本地改动

现有源码 /mnt/f/bak/git_koa-ol 内修改 4 个文件：

- chat/index.html：三模型选项、真实回复角色与等待提示、防止重复发送、附件能力提示。
- functions/api/chat.js：三路转发；GPT 复用 UPSTREAM 配置并转换 Responses 输入/输出；DeepSeek 使用 DEEPSEEK_API_KEY；Qwen 保留原配置；成功和上游失败继续记录 Stats。
- functions/ai/v1/models.js：仅列出 gpt-5.6-sol。
- functions/ai/v1/responses.js：默认及唯一允许模型为 gpt-5.6-sol；保留客户端密钥校验、流式响应和工具字段转发。

外部传入其他模型会得到 400，而不是悄悄替换成 GPT。模型范围写入代码，因此新版不再读取 DEFAULT_MODEL、AVAILABLE_MODELS。

## 验证

- 12 项 Node 测试通过，覆盖三模型路由、用量写库、附件转换、配置缺失、错误响应、外部模型限制、鉴权和流式日志。
- Playwright 使用现有 Chrome 进行本地桌面 1440px、手机 390px 验证；三模型切换/模拟发送/回复标识通过，无横向溢出、无脚本错误。截图及测试脚本位于 verification/。
- 当前线上 GPT 代理真实请求返回 HTTP 200，model 为 gpt-5.6-sol，有文字回复，计量 input 306 / output 5。最初 Python 默认 User-Agent 请求为 403，改用常规 User-Agent 重试成功；未进一步证明具体拦截规则。
- 前一轮 Qwen 官方 qwen-plus 与 DeepSeek 官方 deepseek-chat 均真实调用成功；DeepSeek 返回模型名为 deepseek-flash。
- 图片/PDF 的 GPT 协议转换通过模拟测试，未向真实上游上传图片/PDF验证。Qwen/DeepSeek 当前选择只接受文字和文本文件；有二进制附件的对话切换时会提示。

## 当前阻碍

Cloudflare MCP GET 成功，但 PATCH production.env_vars.DEEPSEEK_API_KEY 返回 10000 Authentication error。没有完成新增密钥，未提交、未推送、未部署。现有线上仍为旧版，不要把本地实现当成线上已生效。

## 上线所需配置与顺序

1. 为 Cloudflare Pages 配置写入取得可用权限，或者在控制台手动增加 Secret DEEPSEEK_API_KEY，值从密码本 deepseek-harness 读取。
2. 核对/更新 QWEN_API_KEY 为已验证的 qwen-dashscope 条目中的密钥，建议保存为 Secret。本次没有证明 Cloudflare 旧密钥与该条目相同。
3. 只提交本次 4 个源码文件并部署；验证网页三个模型、Stats 新日志、外部模型列表和模型拒绝行为。
4. 新版成功后删除 CHAT_PASSWORD、ANTHROPIC_API_KEY、ANTHROPIC_BASE_URL、ANTHROPIC_MODEL、DEFAULT_MODEL、AVAILABLE_MODELS 共 6 项。上线前不要删仍被旧版使用的变量。

最终建议保留 8 个变量：UPSTREAM_BASE_URL、UPSTREAM_API_KEY、CLIENT_API_KEYS、DEEPSEEK_API_KEY、QWEN_BASE_URL、QWEN_API_KEY、QWEN_MODEL、STATS_PASSWORD；另保留数据库绑定 DB。Qwen 三项仍与 Multi-Agent 共用。

## 回退

本地未提交，可按交付补丁核对后恢复本次 4 文件的原内容；不要覆盖仓库原有未跟踪资料。线上配置未变，无需回退。正式部署后若回滚旧代码，应先恢复该旧版本需要的变量，尤其 Claude 及外部默认/列表配置。

协议依据：https://developers.openai.com/api/docs/guides/text
