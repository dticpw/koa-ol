<!doctype html><html lang="zh-CN"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>file-archives-20260912.md</title><style>/* muQ theme contract v1: semantic roles shared by catalogue, reader, reports. */
:root{--paper:#f5f1eb;--white:#fffdf9;--ink:#302936;--muted:#716772;--plum:#665173;--night:#292332;--gold:#dfcba2;--line:#dfd7dc;--muq-reading-width:78ch;--muq-radius:8px;--muq-space:24px;--muq-body-font:"Noto Sans CJK SC","Microsoft YaHei",system-ui,sans-serif;--muq-heading-font:"Noto Serif CJK SC","Songti SC",serif}
/* Namespaced report components, v1. Safe to embed in a standalone report. */
.muq-document{box-sizing:border-box;max-width:var(--muq-reading-width,78ch);margin:40px auto;padding:0 24px;color:var(--ink,#302936);font:16px/1.95 var(--muq-body-font,system-ui);overflow-wrap:anywhere}
.muq-document h1,.muq-document h2,.muq-document h3{font-family:var(--muq-heading-font,serif);line-height:1.5;margin:1.8em 0 .6em}.muq-document h1{font-size:2em}.muq-document h2{font-size:1.5em;border-bottom:1px solid var(--line);padding-bottom:.4em}.muq-document a{color:var(--plum);text-decoration:underline}.muq-document pre{padding:18px;background:var(--paper);overflow:auto;white-space:pre-wrap}.muq-document code{font-size:.9em}.muq-document table{display:block;overflow:auto;border-collapse:collapse;max-width:100%}.muq-document th,.muq-document td{border:1px solid var(--line);padding:10px 14px;text-align:left}.muq-document img{max-width:100%;height:auto}.muq-document blockquote,.muq-callout{margin:24px 0;padding:16px 22px;border-left:3px solid var(--plum);background:var(--paper)}.muq-lead{font-size:1.15em;color:var(--muted)}.muq-source{font-size:.85em;color:var(--muted)}.muq-document :focus-visible{outline:3px solid var(--plum);outline-offset:3px}@media(max-width:540px){.muq-document{margin:20px auto;padding:0 16px;font-size:15px}}@media print{.muq-document{max-width:none;margin:0}.muq-document a{color:inherit}}
</style><main class="muq-document"><h1>文件成果与真实目录归档</h1>
<p>用于选择文件、组织资源、保留多文件作品及验收下载；会话身份、原话采集、可见范围和发布授权沿用 SKILL.md。实现与命令细节以共享项目 <code>docs/content-and-files.md</code> 为准（Linux <code>/mnt/e/PG/muQ</code>，Windows <code>E:/PG/muQ</code>）。</p>
<h2>选择导入方式</h2>
<table>
<thead>
<tr>
<th>需求</th>
<th>方式</th>
<th>范围</th>
</tr>
</thead>
<tbody>
<tr>
<td>单份报告、图片或文件</td>
<td><code>sync --artifact &lt;文件&gt;</code></td>
<td>复制入库；超限自动转为索引</td>
</tr>
<tr>
<td>随会话归档多文件网页等作品</td>
<td><code>sync --directory &lt;目录&gt;</code></td>
<td>保留源目录名与内部层级；超限文件仅登记索引</td>
</tr>
<tr>
<td>现有条目需完整保存选中目录的本地原件</td>
<td><code>import-directory &lt;ID&gt; &lt;目录&gt; --prefix &lt;版本目录名&gt; --revision &lt;当前版本&gt;</code></td>
<td>全部复制到新版本；构建时仍按阈值和 publish:false 筛选</td>
</tr>
<tr>
<td>原件很大或只需知道位置</td>
<td><code>sync --reference-file &lt;文件&gt;</code></td>
<td>仅保存路径、大小及 SHA256，不构成备份</td>
</tr>
</tbody>
</table>
<p>先选择与任务有关的成果范围，避免把项目根目录中的密码本、环境、依赖缓存、Git 数据和无关临时文件整包导入。目录导入不会替你排除这些文件；需要精选时先在任务产物目录准备保持相对关系的成果目录。</p>
<p><code>sync</code> 沿用既有一会话一日记；若先 sync 再 import-directory，读取这次 sync 返回的新 revision。对已有条目先 <code>info ID --brief</code>，需要合并总结与 resources 时读取完整当前条目。导入创建新版本，不自动更新网站或改变可见范围。</p>
<p>目录默认挂在 <code>artifacts/&lt;源目录名&gt;/</code>；<code>import-directory --prefix work-v2</code> 则挂在 <code>artifacts/work-v2/</code>。同路径拒绝覆盖，更新作品使用新的版本目录名，保留历史。同名不同目录可以共存；绝对路径、..、符号链接及跨系统大小写/Unicode 名称冲突会被拒绝，不能通过关掉校验绕过。</p>
<p>历史平铺附件没有原目录证据时保持平铺；不要根据文件名猜层级或用虚拟分组假装原目录。需要恢复目录时从明确的原始来源导入新版本。</p>
<h2>命名与成果组织</h2>
<ul>
<li><code>resources[].title</code> 解释用途，<code>file</code> 使用真实归档相对路径。保留真实文件名和后缀，不为界面简洁改掉实际下载名。</li>
<li>展示采用可读标题、真实文件名、格式和大小；标题与文件名相同则避免重复。长中文、空格、多点后缀应完整可辨，窄屏可换行。</li>
<li>用 <code>resources[].collection</code> 表达明确的成果组合，例如同一作品的原图、生成提示词、生成记录。无可靠组合关系时保持平铺，不靠名称猜配对。</li>
<li><code>collection</code> 是阅读分组；<code>group: files</code> 是把跨来源 link 放进主题文件区；<code>presentation: frontpage</code> 指定 HTML 门面，三者不要混用。</li>
<li>图片等比展示，不拉伸或裁切关键内容。生成提示词附件可以是图像成果；它与真实用户会话原话不同，后者始终私有。</li>
</ul>
<p>示例：源目录 <code>illustration-v2</code> 内有原图和生成提示词，两份作为同一组展示。</p>
<pre><code class="language-json">{
  &quot;resources&quot;: [
    {&quot;kind&quot;: &quot;file&quot;, &quot;file&quot;: &quot;artifacts/illustration-v2/original.png&quot;, &quot;title&quot;: &quot;作品原图&quot;, &quot;collection&quot;: &quot;插画作品&quot;, &quot;featured&quot;: true},
    {&quot;kind&quot;: &quot;file&quot;, &quot;file&quot;: &quot;artifacts/illustration-v2/generation-prompt.txt&quot;, &quot;title&quot;: &quot;生成提示词&quot;, &quot;collection&quot;: &quot;插画作品&quot;}
  ]
}
</code></pre>
<p><code>resources</code> 列表更新会替换整份列表，必须合并保留已有有效成果；示例不能直接覆盖真实条目。要让目录内容可作完整作品预览，保留 HTML、CSS、经典 JS、图片和字体之间的相对关系。</p>
<h2>调用示例</h2>
<p>以下从本 Skill 根目录执行；<code>scripts/muq.py</code> 是跨系统调用器，支持 sync、import-directory、build、publish。把示例 ID、路径和 revision 替换为现场值。</p>
<pre><code class="language-sh"># Linux：同步真实会话，复制所选目录；超限文件转为索引。
AGENTFLOW_CREDENTIALS_FILE=/mnt/e/PG/keys/credentials.md /home/dticpw/conda/envs/py310/bin/python scripts/muq.py sync --provider codex --summary /path/to/summary.json --directory /path/to/illustration-v2

# Linux：给现有条目完整保存本地目录；不自动构建或发布。
/home/dticpw/conda/envs/py310/bin/python scripts/muq.py import-directory ENTRY_ID /path/to/website --prefix website-v2 --revision CURRENT_REVISION
</code></pre>
<p>Windows 使用 <code>D:/python/anaconda/envs/th123/python.exe scripts/muq.py</code>，文件路径换成 Windows 形式；默认凭据路径由 Windows 密码本入口解析。<code>--provider</code> 按真实客户端选择，Claude 还须明确 session-id 或 transcript，不能套用示例的 Codex 身份。</p>
<h2>权限、依赖与验收</h2>
<p>公开/馆主共用阅读模型和文件规划。未知字段不会自动公开；需要新阅读字段时先按项目实现声明用途及权限，不用复制完整 Store 对象绕过投影。普通档案默认私有、AI 月刊默认公开、馆主逐条选择优先；主题公开不连带公开来源日记、原话、本机路径或未获准附件。完全私有条目不为锁定封面而自动暴露存在、标题或数量。</p>
<p>目录视图只来自当前视角获准的归档文件，不是本机文件系统浏览。源修改时间仅在导入时有记录才显示，不拿入库时间替代；目录项数为直接子项。未上传、本地仅索引、缺失依赖须如实标注。</p>
<p>静态/隔离 HTML 只重建已获准附件，保持 sandbox；不得添加 allow-same-origin 或为预览放开联网。外部 CDN、动态模块、CSS @import、fetch 和后端程序不保证可运行，依赖提示也不是任意程序完整性的证明。</p>
<p>交付时打开真实条目，按涉及的能力核对：</p>
<ol>
<li>成果标题、真实文件名、格式、分组和原图比例；桌面及窄屏能读清，必要时检查 1440/820/390 宽度。</li>
<li>有目录时核对层级、面包屑、返回上级与键盘操作；平铺成果不伪造目录。</li>
<li>实际预览和下载至少一份代表文件；多文件作品检查依赖，下载目录 ZIP 并比对相对路径及字节或 SHA256，特别是中文和同名不同目录。</li>
<li>核对公开/解锁视角及未上传说明。目录 ZIP 含下载清单（MUQ-DOWNLOAD-MANIFEST.json，冲突时改名），整条网页 ZIP 含 WEB-MANIFEST.json；只称包含当前已上传清单，不称原项目完整备份。</li>
<li>分别报告入库、本地构建、源码提交与网站发布；原图/索引未上传或浏览器未验收时明确边界。</li>
</ol></main></html>