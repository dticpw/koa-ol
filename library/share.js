'use strict';
(async()=>{const status=document.querySelector('#status');try{const [id,token]=location.hash.slice(1).split('.');history.replaceState(null,'',location.pathname);if(!/^[a-f0-9]{32}$/.test(id||'')||!token)throw Error('请使用包含完整分享标识的链接。');const base='/api/muq-share/'+id+'/';const r=await fetch(base,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})});if(!r.ok)throw Error('分享不存在、已过期或已撤销。');const info=await r.json();status.textContent='有效期至 '+new Date(info.expires).toLocaleString('zh-CN')+' · 分享仅包含馆主选定的内容';const frame=document.querySelector('#view');const show=name=>{frame.src=base+name.split('/').map(encodeURIComponent).join('/');frame.hidden=false;};for(const name of info.files){const b=document.createElement('button');b.textContent=name==='index.html'?'条目门面':name;b.style.cssText='padding:10px;margin:4px;max-width:100%;overflow-wrap:anywhere';b.onclick=()=>show(name);document.querySelector('#files').append(b);}show('index.html');}catch(e){status.textContent=e.message;}})();
