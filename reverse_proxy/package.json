{
  "name": "koa-ol-api-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Claude API proxy",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
